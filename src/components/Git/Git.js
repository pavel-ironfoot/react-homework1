import './Git.css';

const Git = () =>{
    return (
        <div className='git'>
            <h1>Git</h1>
            <p>If you see this page application it means that I done first tusk :)</p>
        </div>
    );
}

export default Git;